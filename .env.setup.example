# Copy to .env.setup and fill (never commit .env.setup)

# Supabase → Account → Access Tokens → Generate new token
SUPABASE_ACCESS_TOKEN=

# Supabase → Project Settings → API → service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=
