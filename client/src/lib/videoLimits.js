/** Max allowed video length for analysis (seconds). */
export const MAX_VIDEO_DURATION_SEC = 120;

/** Client-side tolerance before metadata rounding (matches server check). */
export const MAX_VIDEO_DURATION_TOLERANCE = 125;
